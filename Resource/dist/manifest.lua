-- Generated by CSharp.lua Compiler
return function (out)
  do
    out = (out and #out > 0) and (out .. '.') or ""
    local require = require
    local load = function(module) return require(out .. module) end

    -- load all files
    load("Element")
    load("Program")
    load("Structs")
    load("Vehicle")
  end

  System.init({
    "MTAWrapper.Element",
    "MTAWrapper.Vehicle",
    "src.Program",
    "src.Vector3"
  }, {
    Main = "src.Program.Main"
  })
end
